services:
  frontend:
    build:
      context: ./apps/frontend
    container_name: frontend
    ports:
      - '5173:5173'
    volumes:
      - ./apps/frontend:/app
      - /app/node_modules
    environment:
      - VITE_USE_MOCK=true
    command: sh -c "npm install && npm run dev -- --host"
